# Bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/

# Entornos
.env
.venv/
venv/
env/

# VS Code / notebooks cache
.vscode/
.ipynb_checkpoints/

# Logs (⚠️ permitiremos logs en outputs más abajo)
# *.log   ← quita o comenta esta línea si quieres subir logs globalmente

# Datos / pesos en la RAÍZ (no afecta a hsi_lab/*)
#/data/
#/models/
/saved_models/
/datasets/
# /outputs/  ← NO lo ignores: queremos versionarlo

# PROHIBIR binarios pesados (en cualquier carpeta)
*.h5
*.hdf5
*.ckpt
*.npz
*.npy
*.pt
*.pth
*.zip
*.tar
*.tar.gz

# PERMITIR explícitamente logs y texto dentro de outputs/
!outputs/**
# …pero vuelve a ignorar dentro de outputs cualquier binario pesado:
outputs/*.h5
outputs/*.hdf5
outputs/*.ckpt
outputs/*.npz
outputs/*.npy
outputs/*.pt
outputs/*.pth
outputs/*.zip
outputs/*.tar
outputs/*.tar.gz

# Sistema
.DS_Store
Thumbs.db
