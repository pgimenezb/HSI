Cómo usarlo (sin tocar el archivo)

CLI:

Un modelo:
python -m hsi_lab.train --models cnn_residual --trials 40 --epochs 60

Varios:
python -m hsi_lab.train --models cnn_baseline,dnn_wide --trials 30 --epochs 50 --n-jobs-models 1

ENV (ideal screen):

export HSI_MODELS="cnn_dilated,dnn_selu"
python -m hsi_lab.train